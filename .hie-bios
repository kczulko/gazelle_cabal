#!/usr/bin/env bash
set -euo pipefail
bazel build //cabalscan:hie-bios --output_groups=hie_bios
cat bazel-bin/cabalscan/hie-bios@hie-bios > "$HIE_BIOS_OUTPUT"
# Make warnings non-fatal
echo -Wwarn >> "$HIE_BIOS_OUTPUT"
